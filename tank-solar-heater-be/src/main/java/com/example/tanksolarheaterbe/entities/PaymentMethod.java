package com.example.tanksolarheaterbe.entities;

/** Payment methods offered at checkout. */
public enum PaymentMethod {
    COD,
    /** Online bank transfer, settled through the VNPay gateway. */
    BANK_TRANSFER
}
